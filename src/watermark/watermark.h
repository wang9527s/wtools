#ifndef WATERMARK_H
#define WATERMARK_H

#include <QWidget>

class watermark : public QWidget
{
    Q_OBJECT
public:
    explicit watermark(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // WATERMARK_H